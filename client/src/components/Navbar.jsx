/*
 * Deprecated — the dashboard now ships its own TopBar inline in pages/Dashboard.jsx.
 * Kept as a thin shim to avoid breaking any stray imports.
 */
import React from 'react';

const Navbar = () => null;

export default Navbar;
